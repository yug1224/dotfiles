# Shared basename paths (no extension) that intentionally have no Cursor/Claude wrappers.
# Consumed by check-wrapper-parity.sh and scaffold-wrappers.sh.
conventions/output-verification-rule
# Claude-host-only discovery index (Tier A). No shared SoT / no Cursor wrapper.
INDEX
